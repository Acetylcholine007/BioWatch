import 'package:flutter/material.dart';

class PersonViewer extends StatelessWidget {
  const PersonViewer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Person Viewer'),
      ),
      body: Container(

      ),
    );
  }
}
